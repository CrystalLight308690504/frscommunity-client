<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="XUITheme.Phone">
            <item name="android:textColor">@color/default_displayed_font_gray_color</item>
    </style>

</resources>
